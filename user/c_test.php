<?php
// other_php_file.php
if(isset($_POST['provider_id'])) {
    $provider_id = $_POST['provider_id'];
    // Display the provider_id or perform any other actions with it
    echo $provider_id;
}
?>
